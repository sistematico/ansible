---
- hosts: all
  tasks:
    - name: include ubuntu.yml
      include: ubuntu.yaml

    - name: include letsencrypt.yml
      include: letsencrypt.yaml

    - name: include nginx.yml
      include: nginx.yaml